const INITIAL_STATE = {
  list: [],
  editingItem: null
}

const shoppingListReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default shoppingListReducer
